#compiles the val and training loss, lastEpoch, & data_prep
#save into a master csv file
#pull up each ... file in trained models
#save each of those aspects
test = {"val":[1,2,3,4], "two":[4,5,6,7]}
print(len(test["two"]))
